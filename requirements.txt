# Core dependencies - LangChain and MCP
langchain-core>=0.3.0
langchain-ollama>=0.2.0
langgraph>=0.2.0
langchain-mcp-adapters>=0.1.0

# OpenSearch and ML
opensearch-py>=2.6.0
opensearch-py[async]>=2.6.0  # Async support
sentence-transformers>=2.2.0

# HTTP and Web scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
aiohttp>=3.9.0  # Async HTTP client
aiohttp-client-cache>=0.11.0  # HTTP caching
aiosqlite>=0.19.0  # SQLite backend for cache

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0

# Reliability and retry logic
tenacity>=8.2.3  # Retry with exponential backoff

# MCP Server
mcp>=0.1.0

# Optional: Async tools
nest-asyncio>=1.5.0  # For running async code in Jupyter/notebooks
